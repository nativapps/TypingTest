<header>
    <%= render :partial => "header_view/header_admin" %>
    <h1>ROOM</h1>
    <p>Administrator</p>
</header>

<main>
    <div class="main_wrapper">

      <ul class="clearfix">
        <li><%= link_to image_tag("back2x.png")+"Go back", rooms_path, class:"button_control" %></li>
        <li><%= link_to image_tag("edit2x.png")+"Edit", edit_room_path(@room), class:"button_control" %></li>
        <li><%= link_to image_tag("delete2x.png")+"Delete",  @room, method: :delete, data: {confirm: '¿Do you want to delete this room?'}, class:"button_control" %></li>
      </ul>

      <section class="sub_main">
        <h4>INFORMATION ROOM</h4>
        <table>
          <tr>
            <td><strong>Room Name:</strong> @room.name</td>
            <td><strong>Start Date:</strong> @room.start_date</td>
            <td><strong> Finish Date:</strong> @room.finish_date</td>
          </tr>

          <tr>
            <td><strong>Tried Times:</strong> @room.tried_times</td>
            <td><strong>Limit Time:</strong> @room.set_limit</td>
            <td><strong>Created Date:</strong>@room.created_at</td>
          </tr>
        </table>
      </section>

      <section class="sub_main">
        <h4>TEST</h4>

        <div class="table-wrapper">
          <table>
            <tr>
              <th>Title</th>
              <th>Type</th>
            </tr>
            <% @room.test_banks.each do |t| %>
                <tr>
                    <td><%= t.name %></td>
                    <td><%= t.type_test %></td>
                </tr>
            <% end %>
          </table>
        </div> <!-- .table-wrapper -->
      </section>

      <section class="sub_main">
        <h4>PARTICIPANTS</h4>
        <div class="table-wrapper">
          <table>
            <tr>
              <th>Identification</th>
              <th>First Name</th>
              <th>Last Name</th>
            </tr>
            <% @room.participants.each do |rt| %>
                <tr>
                    <td><%= rt.identification %></td>
                    <td><%= rt.first_name %></td>
                    <td><%= rt.last_name %></td>
                </tr>
                <% end %>
          </table>
        </div> <!-- .table-wrapper -->
      </section>



    </div><!-- .main_wrapper -->
  </main>